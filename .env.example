# Copy to .env and fill in. Never commit the real .env file.

# Required for cde/phase2 (real model adapter / smoke test).
ANTHROPIC_API_KEY=

# Optional overrides — defaults live in cde/phase2/config.js
# CDE_MODEL=claude-sonnet-5
# CDE_TEMPERATURE=0
# CDE_MAX_TOKENS=1024
# CDE_MAX_TOOL_TURNS=8
# CDE_MAX_RETRIES=3
